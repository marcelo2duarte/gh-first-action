name: First Workflow
on: workflow_dispatch
jobs:
  fist-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello World!"
            echo "First step done!"
      - name: Print goodbye
        run: |
            echo "Done - bye!"
            echo "Last step done!"
